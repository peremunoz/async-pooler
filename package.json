{
  "name": "async-pooler",
  "version": "0.1.0",
  "description": "Lightweight and typed async pool manager with concurrency control and retry strategies.",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run test --watch",
    "test": "vitest run",
    "lint": "biome check .",
    "lint:fix": "biome check --apply .",
    "build": "bun build ./src/index.ts --outdir dist --target node"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "biome": "^0.3.3",
    "bun-types": "^1.3.1",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
