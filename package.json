{
	"name": "async-pooler",
	"version": "1.3.0",
	"description": "Lightweight and typed async pool manager with concurrency control and retry strategies.",
	"module": "src/index.ts",
	"type": "module",
	"scripts": {
		"dev": "bun run test --watch",
		"test": "vitest run",
		"lint": "bunx biome check",
		"lint:fix": "bunx biome check --write",
		"build": "bun build ./src/index.ts --outdir dist --target node",
		"docs": "typedoc --out docs src --excludePrivate --excludeProtected --includeVersion"
	},
	"keywords": [
		"async",
		"pool",
		"concurrency",
		"promise",
		"promise manager"
	],
	"author": "Pere Muñoz <peremunozfiguerol33@gmail.com>",
	"license": "MIT",
	"devDependencies": {
		"@biomejs/biome": "2.2.7",
		"@types/bun": "^1.3.1",
		"bun-types": "^1.3.1",
		"conventional-changelog-cli": "^5.0.0",
		"typedoc": "^0.28.14",
		"vitest": "^4.0.2"
	},
	"peerDependencies": {
		"typescript": "^5.9.3"
	}
}
